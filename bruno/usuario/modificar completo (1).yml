info:
  name: modificar completo (1)
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{url}}/usuario/1"
  body:
    type: json
    data: |-
      {
        "nombre": "Mulan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
