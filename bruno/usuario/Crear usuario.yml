info:
  name: Crear usuario
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/usuario"
  body:
    type: json
    data: |-
      {
          "nombre" : "Marcus G",
          "apellidos": "Edmuno",
          "email": "medmundo@example.net"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
