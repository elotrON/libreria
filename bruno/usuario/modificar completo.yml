info:
  name: modificar completo
  type: http
  seq: 5

http:
  method: PUT
  url: "{{url}}/usuario/1"
  body:
    type: json
    data: |-
      {
          "nombre" : "asdfasf",
          "apellidos": "asdfasdf",
          "email": "anuezp@example.net"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
