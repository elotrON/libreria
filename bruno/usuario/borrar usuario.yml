info:
  name: borrar usuario
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/usuarios"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
